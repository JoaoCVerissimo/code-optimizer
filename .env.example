# Database
DATABASE_URL=postgres://optimizer:optimizer_dev@localhost:5432/code_optimizer

# Redis
REDIS_URL=redis://localhost:6379

# Claude API (required for optimization)
ANTHROPIC_API_KEY=your-api-key-here

# API Server
PORT=4000
HOST=0.0.0.0

# Frontend
NEXT_PUBLIC_API_URL=http://localhost:4000

# Worker
SANDBOX_TIMEOUT_MS=30000
BENCHMARK_RUNS=3

# Mock mode — set to "true" to bypass Claude API and Docker sandbox
# Uses realistic mock data for development and testing without an API key
# MOCK_AI=true
